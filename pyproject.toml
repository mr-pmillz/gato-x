[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gato-x"
version = "1.4.0"
description = "GitHub Actions Enumeration and Attack Framework"
readme = "README.md"
authors = [
    {name = "Adnan Khan", email = "adnanthekhan@proton.me"}
]
maintainers = [
    {name = "mr-pmillz"}
]
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12"
]
dependencies = [
    "colorama",
    "httpx[socks,http2]",
    "pyyaml",
    "cryptography",
    "networkx",
    "PyJWT[crypto]"
]

[project.urls]
Homepage = "https://github.com/mr-pmillz/gato-x"
Documentation = "https://mr-pmillz.github.io/gato-x/"
Repository = "https://github.com/mr-pmillz/gato-x"
Issues = "https://github.com/mr-pmillz/gato-x/issues"
Upstream = "https://github.com/AdnaneKhan/gato-x"

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-cov",
    "pytest-asyncio",
    "ruff",
    "pyright",
    "pre-commit"
]
docs = [
    "mkdocs",
    "mkdocs-material"
]
mcp = [
    "fastmcp"
]

[tool.hatch.build.targets.wheel]
packages = [
    "gatox"
]

[tool.pytest.ini_options]
testpaths = ["unit_test"]
python_files = ["test_*.py"]
addopts = "--cov=gatox"
asyncio_mode = "auto"

[tool.coverage.run]
source = ["gatox"]

[tool.coverage.html]
directory = "cov_html"

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by black
    "E203",  # whitespace before ':', conflicts with black
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]

[tool.ruff.lint.isort]
known-first-party = ["gatox"]

[tool.pyright]
include = ["gatox"]
exclude = ["unit_test", "test"]
pythonVersion = "3.10"
typeCheckingMode = "basic"
venvPath = "."
venv = ".venv"

[project.scripts]
gato-x = "gatox.main:entry"
gatox = "gatox.main:entry"
gato-x-mcp = "gatox.mcp.mcp_server:entry"
